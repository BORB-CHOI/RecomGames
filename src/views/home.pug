extends layouts/main
include mixins/gameBlock

block content
    section.home-games
        each item in games
            +gameBlock({
                id:item.id,
                title:item.title,
                platform:item.platform,
                mainImg:item.mainImg,
                genres:item.genres,
            })